<header class="header">
    <h1>FISTO</h1>

    <% if (!user) { %>
        <a href="/login" class="authLink">Log In</a>
    <% } else { %>
        <a href="/logout" class="authLink">Log Out</a>
    <% } %>
</header>